services:
  postgres:
    ports:
      - "5432:5432"
    restart: unless-stopped

  backend:
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    ports:
      - "8050:8050"
    restart: unless-stopped
