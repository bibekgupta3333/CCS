# Shared
POSTGRES_USER=ccs
POSTGRES_PASSWORD=change_me
POSTGRES_DB=ccs_db

# Backend — service name "postgres" resolves inside Docker network
DATABASE_URL=postgresql://ccs:change_me@postgres:5432/ccs_db

# Frontend — service name "backend" resolves inside Docker network
BACKEND_URL=http://backend:8000
